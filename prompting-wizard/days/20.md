# Day 20 — Negative constraints: forbid what actually happened

## Concept

"Don't be too verbose" rules out nothing specific — any output can claim it wasn't verbose, so the constraint never fires. A negative constraint only works when it names the exact failure it forbids, and that failure has to be one you've actually watched happen, not one you're guessing at.

Generic constraints feel like coverage but leave the model to invent its own definition of the thing you didn't want — usually the definition that got you here. "Don't hallucinate" doesn't tell it that it invented a field name last time. "Don't invent a field name that isn't in the source data" does, because it names an incident instead of a category.

Two exclusions, each tied to something the model actually did wrong on this task, do more than ten generic ones. Past that you're forbidding hypothetical failures instead of the ones that cost you time, and the constraint stops being evidence and starts being a hedge.

Here is the test: for each exclusion in your prompt, ask whether you've watched the model actually do that. If you're guessing, cut it.

## Before / After

**Before**

> Write {{TASK}}.

**After**

> Write {{TASK}}. Do not invent a field name that isn't in the source data — last time you added "priority" when it wasn't a column. Do not summarise the whole document before answering the question — last time that pushed the actual answer past what I read.

The first prompt carries no exclusion at all — nothing rules out the two failures it's about to repeat. Both additions name an incident: a specific invented field, a specific habit of burying the answer. Each rules out exactly the failure that happened, not a category around it.

## Exercise

Pick one of your recurring tasks. Write a single prompt for it.

### Novice

Write a prompt for {{TASK}} with one generic exclusion ("don't be verbose", "don't be wrong"), then replace it with two exclusions naming failures you've actually seen the model make on this task.

### Working

Write a prompt for {{TASK}} with exactly two exclusions, each traceable to a specific past failure you could describe in one sentence if asked.

### Advanced

Write a prompt for {{TASK}} where every exclusion cites a real failure you've seen, not a hypothetical one — be ready to say when and how each one happened.

## Rubric

Score against `rubrics.md#negative-constraints`.
